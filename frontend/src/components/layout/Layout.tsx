import { Menu, Search } from 'lucide-react';
import { Outlet, useLocation } from 'react-router-dom';
import { useState } from 'react';
import Sidebar from './Sidebar';
export default function Layout(){ const [mobileOpen,setMobileOpen]=useState(false); const location=useLocation(); const title=location.pathname.startsWith('/admin')?'Quản trị':location.pathname==='/coach'?'Không gian Coach':'Không gian cá nhân'; return <div className="app-shell"><aside className="desktop-sidebar"><Sidebar/></aside>{mobileOpen&&<div className="mobile-drawer"><button className="drawer-backdrop" aria-label="Đóng menu" onClick={()=>setMobileOpen(false)}/><div className="drawer-content"><Sidebar onClose={()=>setMobileOpen(false)}/></div></div>}<div className="app-main"><header className="app-topbar"><div className="topbar-context"><button className="menu-button" onClick={()=>setMobileOpen(true)} aria-label="Mở menu"><Menu size={19}/></button><div><span>GYMFIT / {title}</span><strong>{location.pathname==='/'?'Dashboard':title}</strong></div></div><div className="topbar-tools"><button className="icon-button" aria-label="Tìm kiếm"><Search size={18}/></button><span className="topbar-date">{new Date().toLocaleDateString('vi-VN',{day:'2-digit',month:'short',year:'numeric'})}</span></div></header><main className="app-content"><div className="content-frame" key={location.pathname}><Outlet/></div></main></div></div>; }

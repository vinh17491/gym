import { randomInt } from 'crypto';
import { getPool, sql } from '../../config/database';
import { AppError } from '../../middleware/errorHandler';
import { UserRole } from '../../types';
import { mailService } from '../mail/mail.service';
import type { AdminPaymentStatusInput, AdminPaymentStatusResult, CreateOrderInput, CreateOrderResult, CustomerOrderDetail, CustomerOrderItem, OrderCreationRow, PaymentNotificationInput, PaymentNotificationResult } from './orders.types';

type PaymentOrder={id:number;order_number:string;user_id:number;customer_name:string;customer_email:string;customer_phone:string|null;total_amount:number;currency:string;payment_status:string;payment_provider:string|null;payment_reference:string|null;order_status:string;created_at:Date};
const terminalPayments=['PAID','REFUNDED','PARTIALLY_REFUNDED'];
const transferContent=(orderNumber:string)=>`GYMFIT ${orderNumber}`;
function orderNumber(){const now=new Date();const date=now.toISOString().slice(0,10).replace(/-/g,'');return `GYMFIT-${date}-${randomInt(0,1000000).toString().padStart(6,'0')}`;}

function adminNotice(order:PaymentOrder,reference:string|null){return {to:process.env.ADMIN_NOTIFICATION_EMAIL||'',subject:`[GymFit] Customer transfer notification - ${order.order_number}`,text:`Order number: ${order.order_number}\nOrder ID: ${order.id}\nCustomer name: ${order.customer_name}\nCustomer email: ${order.customer_email}\nCustomer phone: ${order.customer_phone||'—'}\nTotal amount: ${order.total_amount}\nCurrency: ${order.currency}\nPayment reference: ${reference||'—'}\nTransfer content: ${transferContent(order.order_number)}\nCurrent payment status: PENDING\nCreated time: ${order.created_at.toISOString()}\n\nThis is only a customer-submitted notification. Please verify the bank account before confirming PAID.`};}
export const ordersService={
 async createOrder(userId:number,input:CreateOrderInput):Promise<CreateOrderResult>{const pool=await getPool();const tx=pool.transaction();let started=false;try{await tx.begin();started=true;const user=(await tx.request().input('userId',sql.Int,userId).query<{email:string}>('SELECT email FROM dbo.Users WHERE id=@userId AND is_active=1')).recordset[0];if(!user)throw new AppError(404,'User not found');const rows:OrderCreationRow[]=[];for(const item of [...input.items].sort((a,b)=>a.variantId-b.variantId)){const result=await tx.request().input('variantId',sql.Int,item.variantId).query<OrderCreationRow>('SELECT v.id AS variantId,v.product_id AS productId,p.product_name AS productName,v.variant_name AS variantName,v.sku,v.price,v.sale_price AS salePrice,i.on_hand AS onHand,i.reserved FROM dbo.ProductVariants v WITH (UPDLOCK,HOLDLOCK) JOIN dbo.Products p ON p.id=v.product_id JOIN dbo.Inventory i WITH (UPDLOCK,HOLDLOCK) ON i.variant_id=v.id WHERE v.id=@variantId');const row=result.recordset[0];if(!row)throw new AppError(404,`Variant ${item.variantId} or Inventory not found`);if(!row.productId)throw new AppError(404,'Product not found');const state=(await tx.request().input('variantId',sql.Int,item.variantId).query<{variant_active:boolean;product_active:boolean}>('SELECT v.is_active AS variant_active,p.is_active AS product_active FROM dbo.ProductVariants v JOIN dbo.Products p ON p.id=v.product_id WHERE v.id=@variantId')).recordset[0];if(!state?.product_active)throw new AppError(409,'Product is not active');if(!state.variant_active)throw new AppError(409,'Variant is not active');if(row.onHand-row.reserved<item.quantity)throw new AppError(409,`Insufficient stock for variant ${item.variantId}`);rows.push({...row,price:Number(row.salePrice!==null&&row.salePrice<row.price?row.salePrice:row.price)});}const pricing={subtotal:rows.reduce((sum,row,index)=>sum+row.price*input.items[index].quantity,0),discountAmount:0,shippingAmount:0,taxAmount:0,currency:(process.env.ORDER_CURRENCY||'VND').toUpperCase()};pricing.subtotal=Math.round(pricing.subtotal*100)/100;const pricingTotal=pricing.subtotal;const inserted=await tx.request().input('orderNumber',sql.NVarChar(50),orderNumber()).input('userId',sql.Int,userId).input('customerName',sql.NVarChar(200),input.customerName).input('customerEmail',sql.NVarChar(255),user.email).input('customerPhone',sql.NVarChar(50),input.customerPhone).input('line1',sql.NVarChar(255),input.shippingAddressLine1).input('line2',sql.NVarChar(255),input.shippingAddressLine2||null).input('city',sql.NVarChar(100),input.shippingCity).input('state',sql.NVarChar(100),input.shippingState||null).input('postal',sql.NVarChar(30),input.shippingPostalCode||null).input('country',sql.NVarChar(100),input.shippingCountry).input('subtotal',sql.Decimal(18,2),pricing.subtotal).input('discount',sql.Decimal(18,2),pricing.discountAmount).input('shipping',sql.Decimal(18,2),pricing.shippingAmount).input('tax',sql.Decimal(18,2),pricing.taxAmount).input('total',sql.Decimal(18,2),pricingTotal).input('currency',sql.Char(3),pricing.currency).query<{id:number;order_number:string;created_at:Date}>('INSERT dbo.Orders(order_number,user_id,customer_name,customer_email,customer_phone,shipping_address_line1,shipping_address_line2,shipping_city,shipping_state,shipping_postal_code,shipping_country,subtotal,discount_amount,shipping_amount,tax_amount,total_amount,currency,order_status,payment_status,payment_provider,created_at,updated_at) OUTPUT INSERTED.id,INSERTED.order_number,INSERTED.created_at VALUES(@orderNumber,@userId,@customerName,@customerEmail,@customerPhone,@line1,@line2,@city,@state,@postal,@country,@subtotal,@discount,@shipping,@tax,@total,@currency,N\'PENDING\',N\'UNPAID\',N\'BANK_TRANSFER\',SYSUTCDATETIME(),SYSUTCDATETIME())');const order=inserted.recordset[0];for(const item of input.items){const row=rows.find(candidate=>candidate.variantId===item.variantId) as OrderCreationRow;await tx.request().input('orderId',sql.Int,order.id).input('productId',sql.Int,row.productId).input('variantId',sql.Int,row.variantId).input('productName',sql.NVarChar(200),row.productName).input('variantName',sql.NVarChar(200),row.variantName).input('sku',sql.NVarChar(200),row.sku).input('quantity',sql.Int,item.quantity).input('unitPrice',sql.Decimal(18,2),row.price).input('lineTotal',sql.Decimal(18,2),Math.round(row.price*item.quantity*100)/100).query('INSERT dbo.OrderItems(order_id,product_id,variant_id,product_name,variant_name,sku,quantity,unit_price,line_total,created_at) VALUES(@orderId,@productId,@variantId,@productName,@variantName,@sku,@quantity,@unitPrice,@lineTotal,SYSUTCDATETIME())');await tx.request().input('variantId',sql.Int,row.variantId).input('quantity',sql.Int,item.quantity).query('UPDATE dbo.Inventory SET reserved=reserved+@quantity,updated_at=SYSUTCDATETIME() WHERE variant_id=@variantId AND reserved+@quantity<=on_hand');}await tx.commit();started=false;return {id:order.id,orderNumber:order.order_number,orderStatus:'PENDING',paymentStatus:'UNPAID',paymentProvider:'BANK_TRANSFER',subtotal:pricing.subtotal,totalAmount:pricingTotal,currency:pricing.currency,itemCount:input.items.reduce((sum,item)=>sum+item.quantity,0),createdAt:order.created_at};}catch(error:unknown){if(started)await tx.rollback();throw error;}},
 async getCustomerOrder(orderId:number,userId:number,role:UserRole):Promise<CustomerOrderDetail>{const pool=await getPool();const result=await pool.request().input('orderId',sql.Int,orderId).query<CustomerOrderDetail & {user_id:number;order_number:string;customer_name:string;customer_email:string;customer_phone:string|null;shipping_address_line1:string|null;shipping_address_line2:string|null;shipping_city:string|null;shipping_state:string|null;shipping_postal_code:string|null;shipping_country:string|null;payment_provider:string|null;payment_reference:string|null;subtotal:number;discount_amount:number;shipping_amount:number;tax_amount:number;total_amount:number;order_status:string;payment_status:string;created_at:Date;updated_at:Date}>('SELECT id,order_number,user_id,order_status,payment_status,payment_provider,payment_reference,subtotal,discount_amount,shipping_amount,tax_amount,total_amount,currency,created_at,updated_at,customer_name,customer_email,customer_phone,shipping_address_line1,shipping_address_line2,shipping_city,shipping_state,shipping_postal_code,shipping_country FROM dbo.Orders WHERE id=@orderId');const row=result.recordset[0];if(!row)throw new AppError(404,'Order not found');if(role!==UserRole.ADMIN&&row.user_id!==userId)throw new AppError(403,'You are not allowed to view this order');const items=await pool.request().input('orderId',sql.Int,orderId).query<CustomerOrderItem>('SELECT id,product_id AS productId,variant_id AS variantId,product_name AS productName,variant_name AS variantName,sku,quantity,unit_price AS unitPrice,line_total AS lineTotal FROM dbo.OrderItems WHERE order_id=@orderId ORDER BY id ASC');const bankValues=[process.env.BANK_NAME,process.env.BANK_ACCOUNT_NAME,process.env.BANK_ACCOUNT_NUMBER,process.env.BANK_QR_IMAGE_URL];const bankTransfer=bankValues.some(Boolean)?{bankName:process.env.BANK_NAME||null,accountName:process.env.BANK_ACCOUNT_NAME||null,accountNumber:process.env.BANK_ACCOUNT_NUMBER||null,qrImageUrl:process.env.BANK_QR_IMAGE_URL||null,transferContent:`GYMFIT ${row.order_number}`}:{bankName:null,accountName:null,accountNumber:null,qrImageUrl:null,transferContent:`GYMFIT ${row.order_number}`};return {id:row.id,orderNumber:row.order_number,userId:row.user_id,orderStatus:row.order_status,paymentStatus:row.payment_status,paymentProvider:row.payment_provider,paymentReference:row.payment_reference,subtotal:row.subtotal,discountAmount:row.discount_amount,shippingAmount:row.shipping_amount,taxAmount:row.tax_amount,totalAmount:row.total_amount,currency:row.currency,createdAt:row.created_at,updatedAt:row.updated_at,customerName:row.customer_name,customerEmail:row.customer_email,customerPhone:row.customer_phone,addressLine1:row.shipping_address_line1,addressLine2:row.shipping_address_line2,city:row.shipping_city,state:row.shipping_state,postalCode:row.shipping_postal_code,country:row.shipping_country,items:items.recordset,bankTransfer};},
 async notifyPayment(orderId:number,userId:number,role:UserRole,input:PaymentNotificationInput):Promise<PaymentNotificationResult>{const pool=await getPool();const tx=pool.transaction();let started=false;try{await tx.begin();started=true;const result=await tx.request().input('orderId',sql.Int,orderId).query<PaymentOrder>('SELECT id,order_number,user_id,customer_name,customer_email,customer_phone,total_amount,currency,payment_status,payment_provider,payment_reference,order_status,created_at FROM dbo.Orders WITH (UPDLOCK,HOLDLOCK) WHERE id=@orderId');const order=result.recordset[0];if(!order)throw new AppError(404,'Order not found');if(role!==UserRole.ADMIN&&order.user_id!==userId)throw new AppError(403,'You may only update your own order');if(terminalPayments.includes(order.payment_status))throw new AppError(409,`Payment status ${order.payment_status} cannot be changed by customer notification`);if(order.payment_status==='PENDING'){await tx.commit();started=false;return {orderId:order.id,orderNumber:order.order_number,paymentStatus:'PENDING',paymentProvider:'BANK_TRANSFER',emailSent:false};}await tx.request().input('orderId',sql.Int,orderId).input('reference',sql.NVarChar(255),input.paymentReference||null).query("UPDATE dbo.Orders SET payment_provider=N'BANK_TRANSFER',payment_status=N'PENDING',payment_reference=@reference,updated_at=SYSUTCDATETIME() WHERE id=@orderId");await tx.commit();started=false;const email=await mailService.send(adminNotice({...order,payment_status:'PENDING',payment_provider:'BANK_TRANSFER',payment_reference:input.paymentReference||null},input.paymentReference||null));return {orderId:order.id,orderNumber:order.order_number,paymentStatus:'PENDING',paymentProvider:'BANK_TRANSFER',emailSent:email.sent};}catch(error:unknown){if(started)await tx.rollback();throw error;}},
 async updatePaymentStatus(orderId:number,_adminId:number,input:AdminPaymentStatusInput):Promise<AdminPaymentStatusResult>{const pool=await getPool();const tx=pool.transaction();await tx.begin();try{const result=await tx.request().input('orderId',sql.Int,orderId).query<PaymentOrder>('SELECT id,order_number,user_id,customer_name,customer_email,customer_phone,total_amount,currency,payment_status,payment_provider,payment_reference,order_status,created_at FROM dbo.Orders WITH (UPDLOCK,HOLDLOCK) WHERE id=@orderId');const order=result.recordset[0];if(!order)throw new AppError(404,'Order not found');const previous=order.payment_status;const allowed=(previous==='PENDING'&&(input.status==='PAID'||input.status==='FAILED'))||(previous==='FAILED'&&input.status==='UNPAID')||(previous==='UNPAID'&&input.status==='PAID');if(!allowed)throw new AppError(409,`Invalid payment status transition: ${previous} to ${input.status}`);await tx.request().input('orderId',sql.Int,orderId).input('status',sql.NVarChar(30),input.status).query('UPDATE dbo.Orders SET payment_status=@status,updated_at=SYSUTCDATETIME() WHERE id=@orderId');await tx.commit();const email=input.status==='PAID'||input.status==='FAILED'?await mailService.send({to:order.customer_email,subject:input.status==='PAID'?`[GymFit] Payment confirmed - ${order.order_number}`:`[GymFit] Payment could not be confirmed - ${order.order_number}`,text:input.status==='PAID'?`Order number: ${order.order_number}\nTotal amount: ${order.total_amount} ${order.currency}\nPayment status: PAID\nOrder status: ${order.order_status}\nConfirmation time: ${new Date().toISOString()}`:`Order number: ${order.order_number}\nPayment status: FAILED\nPlease check the amount and transfer content, then contact Admin if needed.`}):{sent:false};return {orderId:order.id,orderNumber:order.order_number,previousPaymentStatus:previous,paymentStatus:input.status,emailSent:email.sent};}catch(error:unknown){await tx.rollback();throw error;}},
};
